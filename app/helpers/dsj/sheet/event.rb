module Dsj::Sheet::Event
  extend ActiveSupport::Concern

  # The participations tab needs a different label for Event::Fundraising,
  # why we have to remove the tab and insert two
  included do
  end
end
